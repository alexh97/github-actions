on: 
  pull_request:
    types: [labeled, unlabeled]

jobs:
  assign_to_project:
    runs-on: ubuntu-latest
    name: Assign a PR to project based on label
    steps:
      - name: Assign to project
        uses: alexh97/github-actions/project-assigner@880e8c861bf1fbdc97a405f1765d7759d89008f4
        id: project_assigner
        with:
          issue-mappings: |
            [
            ]
            
          ghToken: ${{ secrets.PROJECT_ASSIGNER_TOKEN }}

#              {"label": "Test", "projectName": "Test", "columnId": 8230518},
#              {"label": "bug", "projectName": "Test", "columnId": 8230520},
#              {"label": "invalid", "projectName": "Test", "columnId": 8230521}
